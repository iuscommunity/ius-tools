
SOURCE INSTALLATION:

    $ sudo python setup.py install

    # The following is one command
    $ sudo cp -a ./etc/iustools/plugins.d/ossvt.conf \
                 /etc/iustools/plugins.d/
    
    
USAGE:

    $ iustools --help
    
    $ iustools ossvt --help